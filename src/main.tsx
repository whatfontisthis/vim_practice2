import React from 'react'
import ReactDOM from 'react-dom/client'
import VimPractice from '../vim-practice-app.tsx'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <VimPractice />
  </React.StrictMode>,
)
